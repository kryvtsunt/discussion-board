<% include partials/header %>

<h1> List of all items </h1>
<% items.forEach(item => { %>
  <div>
    <h4>
      <%= item.name %>
    </h4>
    <img src="<%= item.image %>">
  </div>
<% }); %>

<% include partials/footer %>
